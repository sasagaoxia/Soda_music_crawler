解决白色空白页面弹窗：

1. 默认启用 Chrome 无头模式：HEADLESS = True。
2. 新版 Chrome 优先使用 --headless=new。
3. 如果新版无头模式启动失败，自动回退到 --headless。
4. Windows 下隐藏 chromedriver 控制台窗口。
5. 即使手动关闭无头模式，也会默认把 Chrome 窗口移到屏幕外，避免白色页面挡屏幕。

配置位置：src/config.py

如需调试显示浏览器窗口：
HEADLESS = False
HIDE_BROWSER_WINDOW = False
